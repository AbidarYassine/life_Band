<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lifeband">

    <application
        android:allowBackup="true"
        android:icon="@drawable/lifeband_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LifeBand">
        <activity android:name=".ui.History">

        </activity>
        <activity
            android:name=".ui.Update"
            android:parentActivityName=".ui.Home" />
        <activity
            android:name=".ui.SignUp"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.LogIn"
            android:parentActivityName=".ui.MainActivity" />
        <activity android:name=".ui.Home" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>